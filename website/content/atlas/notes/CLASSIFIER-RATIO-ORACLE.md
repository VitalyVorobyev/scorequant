---
entity: CLASSIFIER-RATIO-ORACLE
kind: claim
---
With known class priors, calibrated classifier posterior odds recover component density ratios, which can then be transformed into the local mixture score. This continues the same simulation-based-inference line as the local-score identity (`RATIO-LOCAL-SCORE`), using a classifier's posterior odds rather than an explicit density model.
