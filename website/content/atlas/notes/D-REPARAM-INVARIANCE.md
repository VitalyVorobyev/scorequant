---
entity: D-REPARAM-INVARIANCE
kind: claim
---
The determinant criterion is invariant under invertible reparameterization: changing the parameter basis changes the objective by the same constant for every rule, so the ranking of rules is untouched. This is a known property rather than a contribution, but it is a practical reason to prefer the determinant criterion over alternatives that lack it.
