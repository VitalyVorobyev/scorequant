---
entity: RATIO-LOCAL-SCORE
kind: claim
---
The local score is the gradient of a log density ratio, so absolute normalized densities are unnecessary when the relevant local density ratio is available. This is direct prior art from the simulation-based-inference literature, which supplies the local score as a learned summary built on calibrated classifier likelihood ratios.
